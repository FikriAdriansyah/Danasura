@model IEnumerable<Danasura_Project.Models.msKategoriBarang>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin2.cshtml";
    var nomor = 0;
}

<h2>Data Kategori Barang</h2>

<p>
    @Html.ActionLink("Tambah Data Kategori", "Create", new { area = "" }, htmlAttributes: new { @class = "btn btn-success" })
</p>
<table class="table" id="tabelKategori">
    <thead>
        <tr>
            <th>
                No.
            </th>
            <th>
                @Html.DisplayNameFor(model => model.nama)
            </th>
            <th></th>
        </tr>
    </thead>

    @foreach (var item in Model)
    {
        if (item.status == 1)
        {
            nomor++;
            <tr>
                <td>
                    @nomor
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.nama)
                </td>
                <td>
                    @Html.ActionLink("Ubah", "Edit", new { id = item.id_kategori }, htmlAttributes: new { @class = "btn btn-warning" }) |
                    @Html.ActionLink("Hapus", "Delete", new { id = item.id_kategori }, htmlAttributes: new { @class = "btn btn-danger" })
                </td>
            </tr>
        }
    }

</table>

<link href="~/Content/DataTables/datatables.css" rel="stylesheet" />
@*<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet" />*@

@section scripts{
    <script src="~/Content/DataTables/datatablesjs.js"></script>
    @*<script src="https://cdn.datatables.net/1.10.15/js/datatables.bootstrap.min.js"></script>*@

    <script>
        var Popup, dataTable;
        var no;
        $(document).ready(function () {
            dataTable = $("#tabelKategori").DataTable();
        });
    </script>
}